# FastAPI and web server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
aiofiles>=23.2.1

# Add parent directory to path for prdgen imports
# (Install existing prdgen dependencies from parent requirements.txt)
