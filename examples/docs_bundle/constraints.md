Constraints / Requirements:
- Support up to 50M events/day aggregated across tenants
- Provide role-based access (admin, analyst, viewer)
- Export to CSV, and optional warehouse sync later
- Must include dashboards for funnel, cohorts, revenue
